if type shopt > /dev/null; then
  shopt -s expand_aliases
fi
echo "executing command:"
echo "echo './So.many.periods.txt' './SO.MANY.PERIODS.TXT'"
echo './So.many.periods.txt' './SO.MANY.PERIODS.TXT'
exit $?;
