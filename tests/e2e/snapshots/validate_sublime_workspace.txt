if type shopt > /dev/null; then
  shopt -s expand_aliases
fi
echo "executing command:"
echo "echo '{{PROJECT_ROOT}}/tests/inputs/blogredesign.sublime-workspace'"
echo '{{PROJECT_ROOT}}/tests/inputs/blogredesign.sublime-workspace'
exit $?;
