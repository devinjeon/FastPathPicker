if type shopt > /dev/null; then
  shopt -s expand_aliases
fi
echo "executing command:"
echo "wc -l '{{PROJECT_ROOT}}/src/main.rs' '{{PROJECT_ROOT}}/src/parse.rs'"
wc -l '{{PROJECT_ROOT}}/src/main.rs' '{{PROJECT_ROOT}}/src/parse.rs'
exit $?;
