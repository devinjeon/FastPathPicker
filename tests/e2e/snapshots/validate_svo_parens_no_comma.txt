if type shopt > /dev/null; then
  shopt -s expand_aliases
fi
echo "executing command:"
echo "echo '{{PROJECT_ROOT}}/tests/inputs/svo (install the zip not me).xml'"
echo '{{PROJECT_ROOT}}/tests/inputs/svo (install the zip not me).xml'
exit $?;
