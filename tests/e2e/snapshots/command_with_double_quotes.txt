if type shopt > /dev/null; then
  shopt -s expand_aliases
fi
echo "executing command:"
echo "grep \"hello world\" '{{PROJECT_ROOT}}/src/main.rs'"
grep "hello world" '{{PROJECT_ROOT}}/src/main.rs'
exit $?;
