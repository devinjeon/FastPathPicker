if type shopt > /dev/null; then
  shopt -s expand_aliases
fi
echo "executing command:"
echo "echo '{{HOME}}/src/categories/NSDate+Category.h'"
echo '{{HOME}}/src/categories/NSDate+Category.h'
exit $?;
