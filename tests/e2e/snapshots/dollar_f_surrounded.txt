if type shopt > /dev/null; then
  shopt -s expand_aliases
fi
echo "executing command:"
echo "echo before '{{PROJECT_ROOT}}/src/main.rs' after"
echo before '{{PROJECT_ROOT}}/src/main.rs' after
exit $?;
