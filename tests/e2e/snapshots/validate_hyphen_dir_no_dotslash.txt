if type shopt > /dev/null; then
  shopt -s expand_aliases
fi
echo "executing command:"
echo "echo './annoying-hyphen-dir/Package Control.system-bundle'"
echo './annoying-hyphen-dir/Package Control.system-bundle'
exit $?;
